# Web framework
Flask==3.1.1
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0

# Production WSGI server
gunicorn==23.0.0

# Auth / forms / CSRF
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.2

# Database (Supabase / PostgreSQL)
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.42
Flask-Migrate==4.1.0
psycopg2-binary==2.9.9

# Mail + async tasks
Flask-Mail==0.10.0
celery==5.5.3
redis==6.2.0

# PDF generation (reportlab is pure-Python — no system libraries required).
# weasyprint / xhtml2pdf were removed: nothing imported them. If you later add
# HTML->PDF rendering, re-add weasyprint AND its native libs (pango/cairo/...).
reportlab==4.4.3

# Config
python-dotenv==1.1.1

# Error monitoring (optional — only active when SENTRY_DSN is set)
sentry-sdk[flask]==2.67.1
